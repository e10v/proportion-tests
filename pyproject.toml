[project]
name = "proportion_tests"
version = "0.1.0"
description = "Comparison of two-sample proportion tests"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["polars", "tea_tasting", "tqdm"]

[dependency-groups]
dev = ["ruff", "ty"]
